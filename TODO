
smarter build system (see baselib)

	detect byte ordering (little-endian, big-endian)
	ensure byte IS 1, short IS 2 and int IS 4, FAIL if no
	detect 64-bit type availability
	typedef 64-bit type

list

	list alloc with tail (incref on all lists)
	list free with tail (decref on all lists)
	list traverse with tail example
	list reverse
	list duplicate
	list flatten
